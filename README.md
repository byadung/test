
  # Create as is

  This is a code bundle for Create as is. The original project is available at https://www.figma.com/design/2karRlBXewdaBCHSFfxFkG/Create-as-is.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  